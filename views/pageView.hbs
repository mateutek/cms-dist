<h1>{{title}}</h1>

<ul>
    {{#each posts as |post|}}
        <li>
            <h2>{{post.title}}</h2>
            <span>{{post.createdAt}}</span>
            <div>{{post.body}}</div>
        </li>
    {{/each}}
</ul>